import 'package:flutter/material.dart';

class IrsPaymentView extends StatelessWidget {
  const IrsPaymentView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Irs payment'),
      ),
    );
  }
}